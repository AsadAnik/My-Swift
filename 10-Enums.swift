// HOW TO CREATE AND USE ENUMS..
enum Weekday {
    case monday
    case tuesday
    case wednesday
    case thursday
    case friday
};

let day = Weekday.monday;
print(day);

// EASY WAY OF DOING..
enum Color { 
    case red, green, blue, brown, purple, pink, gray, white, black 
};

let myFavColor = Color.red;
print(myFavColor);

